<Page
    x:Class="RadarrApp.Views.Movies"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RadarrApp.Views"
    xmlns:local1="using:RadarrSharp.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}">

    <Grid>
        <TextBlock HorizontalAlignment="Left" Margin="40,40,0,0" FontSize="36" Text="Movies" TextWrapping="Wrap" VerticalAlignment="Top"/>
        <ProgressRing Name="progressRing" Height="80" Width="97" IsActive="True"/>
        <GridView 
            x:Name="ContentGridView"
            IsItemClickEnabled="True"
            CanDragItems="False"
            AllowDrop="False"
            CanReorderItems="False"
            SelectionMode="Single" 
            FlowDirection="LeftToRight" 
            Margin="40,121,0,0"
            ItemClick="ContentGridView_ItemClick">
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="local1:Movie">
                    <StackPanel>
                        <Image Stretch="UniformToFill" Source="{x:Bind Images[0].Url}" Width="167" Height="250" Margin="0,4,0,0"/>
                        <TextBlock Text="{x:Bind Title}" TextTrimming="WordEllipsis"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>

    </Grid>
    
</Page>
